问题：

插件版本启动的时候，假如有俩插件名相同的，就会报异常，插件名只能唯一，不会对版本进行检查。。。

插件名不一样，不同插件名加载，但是插件里的实现不一样，但是对应的接口都一样，这种只关联一个默认的 ，
这时候需要关联，是不是得指定对应的插件名


热启动的思路，就是先通过插件目录进行监听，如果有新的文件就进行插件检查，插件加载。接着就是有一个问题是插件加载的时候如果是高版本，进行升级，
如果是低版本，则这个时候，能不能切换为低版本。这就有一个问题，低版本或者高版本，可不可以都加载进去，可以手动在页面上进行开启关闭。

